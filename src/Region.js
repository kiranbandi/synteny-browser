export default function (newChrom, newStart, newEnd, newID, newOrd) {
    this.chromosome = newChrom;
    this.start = newStart;
    this.end = newEnd;
    this.id = newID;
    this.ordinal = newOrd;
};
